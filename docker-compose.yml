version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gotogether-backend
    ports:
      - '10000:10000'
    env_file:
      - ./backend/.env
    restart: always
